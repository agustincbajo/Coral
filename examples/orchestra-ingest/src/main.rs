//! orchestra-ingest — placeholder microservice.
//!
//! This is a reference Coral consumer. The point of this project is the
//! `.wiki/` + `.github/workflows/` setup, not the runtime code. A real
//! consumer would have an actual HTTP server here (axum, actix, warp,
//! whatever). The skeleton just prints its name so `cargo run` works on
//! a fresh clone.

fn main() {
    println!("orchestra-ingest — see .wiki/ for the LLM-maintained docs");
}
